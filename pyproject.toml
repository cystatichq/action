[project]
name = "action"
version = "0.1.0"
description = "A GitHub Action to perform static analysis on Python code."
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["requests>=2.33.0"]
